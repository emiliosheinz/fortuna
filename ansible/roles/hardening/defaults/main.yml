---
# Public SSH key the GHA deploy job (and the operator workstation) uses to
# reach the deploy user. The site playbook expects this to be provided via:
#   ansible-playbook site.yml -e "hardening_deploy_ssh_public_key=$(cat ~/.ssh/fortuna_deploy.pub)"
# or via the inventory's group_vars override.
hardening_deploy_ssh_public_key: ""
