---
galaxy_info:
  role_name: hardening
  author: emiliosheinz
  description: Deploy user, SSH daemon hardening, ufw rules, restricted sudoers.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
dependencies:
  - role: common
